# What's here?
  Scripts for pre-processing of inputs, datasets, executables, etc.

# Brief description:

| File name | Brief description |
| :--       | --: |
| `compile_ufs.sh` | To build the UFS in Data Atmosphere (DATM) mode |
| |

# Example usage:

- `./compile_ufs.sh`
  Note: No arguments are needed.
