if comiling for the SERIAL job, use
makefile makefile_serial
and in dimensions.h:

      parameter (iqr=1,jqr=1)  ! for serial job

When compiling for the MPI job, 
makefile makefile_mpy
and in dimensions.h:

     parameter (iqr=10,jqr=10)  ! multiple tiles (TYPE=ompi or mpi or shmem)

DON'T FORGET TO MOVE .MOD IN THE CORRESPONDING DIRECTORY
AFTER COMPILING!!!!!!!! makefile does not do it just yet. 